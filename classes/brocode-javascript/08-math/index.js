// Math = built-in object that provides a collection of properties and methods

console.log(Math.PI);
console.log(Math.E);

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x);      // rounds number
// z = Math.floor(x);      // rounds down
// z = Math.ceil(x);       // rounds up
// z = Math.trunc(x);      // removes decimals
// z = Math.pow(x, y);     // power
// z = Math.sqrt(x);       // square root
// z = Math.log(x);        // logarithm
// z = Math.sin(x);        // sine
// z = Math.cos(x);        // cosine
// z = Math.tan(x);        // tangent
// z = Math.abs(x);        // absolute value
// z = Math.sign(x);       // gives the sign (1, -1)

// console.log(z);

// --------------------------------------------------------

let max = Math.max(x, y, z);
let min = Math.min(x, y, z);

console.log(max);
console.log(min);